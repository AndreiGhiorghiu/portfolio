import type { GetOffsetFn } from './types';
/** Any valid CSS string value
 * @example `calc(var(--grid) * 2)
 */
type CSSValue = string;
export declare function offsetFromPointer(point: {
    x: CSSValue;
    y: CSSValue;
}): GetOffsetFn;
export {};
